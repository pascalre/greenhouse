# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MyGarden' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyGarden
  pod 'Charts'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'DZNEmptyDataSet'

  target 'MyGardenTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MyGardenUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
